import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.himanshu',
  appName: 'vaccine-portal',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
